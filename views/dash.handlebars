<div class="row">
    <div class="col-12">
        <h1>{{name}}</h1>
    </div>
</div>

<div class="row">
    <div class="col-4">
        <h3>Stats</h3>
    </div>
    <div class="col-8">
        {{#each location}}
            <button id={{id}}>{{name}}</button>
        {{/each}}
    </div>
</div>